# Configuración de build para Railway / Nixpacks.
# La app vive en backend/, así que instalamos y arrancamos desde ahí.
providers = ["node"]

[phases.install]
cmds = ["cd backend && npm install"]

[start]
cmd = "cd backend && npm start"
