{
  "name": "control-m-mdemateria",
  "version": "0.2.0",
  "private": true,
  "description": "Control M · Producción — m de materia. Scripts de arranque y tests E2E.",
  "scripts": {
    "start": "node backend/server.js",
    "test": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.56.1"
  }
}
